Instructions:

1. create server.js and package.json (!ALL in the same folder as index.html)
2. in termminal (!!! in the same folder as index.html) run commands:
    npm i ws
    node server.js


In the therminal the server is now running, to stop it press ctrl+c
when running node server.js make sure the file's name is the same as the name mentioned in the terminal